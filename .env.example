# Server Configuration
PORT=5000
NODE_ENV=development

# Authentication
JWT_SECRET=raksha360_super_secret_jwt_key_sih_2026

# Database (MongoDB Atlas or local URI)
# If left blank or unreachable, Raksha360 automatically falls back to embedded persistent storage
MONGODB_URI=

# Automated Email Service (Resend / SMTP)
RESEND_API_KEY=
ALERT_FROM_NAME="Raksha360 Early Warning System"
ALERT_FROM_EMAIL=onboarding@resend.dev

# Fallback SMTP settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Default Demo Authority Credentials (pre-seeded)
DEFAULT_AUTHORITY_EMAIL=authority@raksha360.gov.in
DEFAULT_AUTHORITY_PASS=Admin@123
